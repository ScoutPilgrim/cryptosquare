# Cryptosquare practice

What it can do:
* Uses a cryptosquare to encrypt a sentence
* Uses logic surrounding the attributes of two dimensional arrays in order decrypt the aforementioned sentence if the user so chooses
